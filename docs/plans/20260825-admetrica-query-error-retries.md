# Повторы для HTTP 400 `query_error` и точность диагностики отказов

## Overview

Отчётный эндпоинт AdMetrica отвечает с большим разбросом времени и, не уложившись
примерно в десять секунд, отдаёт HTTP 400 с текстом
`Query is too complicated. Please reduce the date interval or sampling.`
Провайдер считает 400 окончательным отказом, поэтому одна такая кампания роняет
таску целиком, а ретрай Airflow пересобирает день заново — все сотни кампаний,
с теми же сотнями шансов споткнуться.

План делает этот отказ повторяемым на уровне одного запроса и попутно закрывает
четыре расхождения между тем, что провайдер делает, и тем, что он о себе говорит.

**Что это даёт.** При одном проценте отказов на запрос и трёхстах кампаниях
вероятность довести прогон до конца поднимается с 14 % (три полных прогона
Airflow) до 99.9997 % (четыре попытки на запрос).

Вторая цифра — верхняя граница, взятая в предположении, что попытки падают
независимо. Наблюдаемый дрейф идёт на масштабе минут, а лестница укладывается в
65 секунд, то есть внутрь этого масштаба, — значит попытки коррелированы и
реальная величина ниже. Насколько — покажет доля `retryable_error` в Loki после
выкатки.

**На чём держится литерал `query_error`.** Наблюдённое тело отказа, дословно:

```json
{"errors":[{"error_type":"query_error","message":"Query is too complicated. Please reduce the date interval or sampling."}],"code":400,"message":"Query is too complicated. Please reduce the date interval or sampling."}
```

Это единственное основание для `_RETRYABLE_ERROR_TYPES` и для фикстур тестов. У
API есть и другие значения — `invalid_token` в фикстурах `tests/test_hook_meta.py:117`,
— поэтому проверять надо принадлежность множеству, а не «любой 400 с телом».

**Что установлено опытом, а не догадкой.** Отказ транзиентный: кампания 56904 за
2025-08-20 ответила за 641 мс в 15:26 и упёрлась в дедлайн в 15:37 с теми же
параметрами. Лестница из семи запросов и замеры 4 × 10 по кампании 81144 вернули
200 на всех вариантах запроса, включая точную копию того, что шлёт провайдер, —
значит запрос корректен, а отказ зависит от момента. Ни `accuracy`, ни число
группировок и метрик на исход не влияют.

## Context (from discovery)

**Файлы:**

- `airflow_provider_yandex_admetrica/hooks/yandex_admetrica.py` (2837 строк) — весь HTTP-путь и диагностика
- `airflow_provider_yandex_admetrica/operators/stats.py` (351 строка) — сигнатура оператора
- `tests/test_hook_http.py` — ветки статусов, лестница, строки лога
- `tests/test_diagnostics.py` — набор ключей события (`EVENT_KEYS`, :576) и версия схемы
- `tests/test_token_never_leaves.py` — шлюз маскирования
- `tests/test_readme.py` — держит README и README_RU в согласии с кодом
- `README.md`, `README_RU.md`, `CONTEXT.md`

**Точки правки в хуке:**

| Строка | Что там |
|---|---|
| `:44` | `_SCHEMA_VERSION = 1` |
| `:216` | `_RETRY_STATUSES = frozenset({429, *range(500, 600)})` |
| `:223` | `_BACKOFF_DELAYS = [1, 2, 4]` |
| `:241` | `_RETRIED_OUTCOMES = frozenset({"retryable_error", "network_error"})` |
| `:726` | `_new_event` — набор полей события |
| `:924` | `_summarize_error` — читает только `code` и `message` |
| `:953` | `_describe_error` — собирает фразу об отказе |
| `:1052` | `_log_attempt` — строка в логе таски |
| `:1072` | `_stamp_response_error` |
| `:1254` | `_event_level` — warn/error по попытке |
| `:2202` | ветка `401` в `_request_page` |
| `:2214` | ветка `_RETRY_STATUSES` |
| `:2236` | ветка `else` → `http_error` |
| `:2524` | докстринг `get_stats` про `sort` |

**Что тесты уже стерегут:**

- `EVENT_KEYS` — событие обязано нести ровно этот набор ключей, новое поле надо добавить в обе стороны.
- `test_readme.py::test_table_names_every_parameter` — таблицы README называют каждый параметр оператора.
- Шлюз токена — любое новое поле, взятое из тела ответа, обязано пройти `_safe_text`.

**Уже проверено и в план не входит:**

- `sort`, `include_undefined`, `limit`, `offset`, `accuracy` — все документированы и в пределах лимитов; замеры не выявили влияния ни одного из них на долю отказов.
- Делать `sort` управляемым не нужно: разница между вариантами оказалась меньше разброса внутри варианта.

## Development Approach

- **testing approach**: Regular — правка, затем тесты на неё в том же шаге
- каждый шаг доводится до конца перед началом следующего
- **каждый шаг обязан заканчиваться тестами** на новый и изменённый код, включая ветки отказов
- **все тесты зелёные перед следующим шагом**, без исключений
- команда тестов: `.venv/bin/pytest tests/ -q`
- **правило комментариев проекта**: комментарий описывает настоящее. Никаких «раньше здесь было», «теперь вместо» и упоминаний удалённого — история живёт в сообщении коммита
- обратная совместимость: поля события только добавляются, ничего не переименовывается и не удаляется

## Testing Strategy

- **unit-тесты** обязательны в каждом шаге
- **тест на утечку токена** обязателен для каждого нового поля, взятого из ответа сервера (`tests/test_token_never_leaves.py`)
- **проверка теста откатом**: для шагов 3, 5 и 6 после написания теста временно отменить правку и убедиться, что тест краснеет. Так уже проверялись правки в этом проекте, и это единственный способ отличить тест, который ловит дефект, от теста, зелёного по случайной причине
- e2e-тестов в проекте нет; примеры DAG покрыты `tests/test_example_*.py` и правками не затрагиваются

## Progress Tracking

- отмечать `[x]` сразу по завершении
- новые задачи — с префиксом ➕
- блокеры — с префиксом ⚠️
- держать план в согласии с фактической работой

## Solution Overview

**Классификация вместо статуса.** Сейчас решение о повторе принимается по коду
ответа: `_RETRY_STATUSES` содержит 429 и весь 5xx. Для 400 кода недостаточно —
повторять нужно только `query_error`, а не любую ошибку разбора запроса. Значит
тело отказа надо прочитать **до** решения о повторе, и `error_type` становится
частью классификации, а не только частью отчёта.

**Исход `retryable_error`, а не новый.** Повторяемый 400 получает тот же исход,
что 429 и 5xx. Это не косметика: `_event_level` (`:1254`) выдаёт `warn` для
непоследних попыток исходов из `_RETRIED_OUTCOMES` и `error` для последней —
ровно то поведение, которое требуется в Loki. Если оставить `http_error`,
каждая попытка ушла бы как `error`.

Отличать этот отказ от 503 дашборд будет по `http_status` и по новому
`error_type`: `outcome` называет политику («это мы повторяем»), а статус и тип —
факт.

**Своя лестница.** Дрейф идёт на масштабе минут, а `[1, 2, 4]` укладывается в
семь секунд, то есть целиком в одно плохое окно. Для `query_error` — отдельная
`[5, 15, 45]`, размах 65 секунд. Обычные 429 и 5xx остаются на прежней: у них
другая природа, а 429 и так слушается `Retry-After`.

**Почему это не разгоняет стоимость прогона.** Запрос, исчерпавший попытки,
роняет таску. В настоящей аварии таска умирает на первой же кампании, а не
перебирает триста штук по 65 секунд. Платим только за кампании, которые в итоге
прошли: при одном проценте отказов на триста кампаний это около трёх повторов,
секунд двадцать на прогон.

## Technical Details

**Новое поле события** (`schema_version` 1 → 2):

```json
{"schema_version": 2, "error_code": null, "error_type": "query_error",
 "error_message": "Query is too complicated. Please reduce the date interval or sampling."}
```

`error_code` остаётся пустым для таких ответов и это правильно: `_find_error`
берёт `errors[0]`, а там `code` нет — он лежит на верхнем уровне тела. Поле
`error_type` заполняет ровно эту дыру.

**Константы:**

```python
_BACKOFF_DELAYS      = [1, 2, 4]     # 429, 5xx, отказ сети
_QUERY_ERROR_DELAYS  = [5, 15, 45]   # HTTP 400 с error_type query_error
_AUTH_STATUSES       = frozenset({401, 403})
_RETRYABLE_ERROR_TYPES = frozenset({"query_error"})
```

**Инвариант, который обязан быть под тестом:** обе лестницы одной длины.
`max_attempts` считается как `len(_BACKOFF_DELAYS) + 1`, и лестница другой длины
рассогласовала бы номер попытки с индексом ступени.

**Порядок в ветке `else` у `_request_page`.** Тело читается до решения, а решение
принимается по **возвращённому значению**, не по полю события. Это записанное
правило шва (`CONTEXT.md:80–82`): «чтение события обратно сделало бы схему
диагностики частью поведения выгрузки, и правка набора полей правила бы то, что
провайдер делает». Поэтому `_stamp_response_error` возвращает тройку, которую
проставил, и развилка читает её:

```python
else:
    _code, error_type, _message = _stamp_response_error(event, resp, token)
    if resp.status_code == 400 and error_type in _RETRYABLE_ERROR_TYPES:
        event["outcome"] = "retryable_error"
        if last_attempt:
            raise _MaskedError(...)
        retry_delay = _QUERY_ERROR_DELAYS[attempt]
    else:
        event["outcome"] = "http_error"
        raise _MaskedError(...)
```

**Строка лога.** Дробь `attempt N/M` осмысленна только там, где лестница
применима, то есть для исходов из `_RETRIED_OUTCOMES`. Для остальных строка
называет окончательность:

```
AdMetrica stat campaign_id=50158 date=2026-08-23 offset=1: failed, not retryable — HTTP 400, invalid_parameter, ...
```

**Строка о восстановлении** пишется в `finally` у `_request_page`, когда исход
`success` и попытка не первая. На первой удачной попытке не пишется ничего, так
что здоровый прогон лог не меняет:

```
AdMetrica stat campaign_id=75098 date=2026-08-23 offset=1: recovered on attempt 3/4
```

## What Goes Where

- **Implementation Steps** — правки кода, тесты, документация в этом репозитории
- **Post-Completion** — то, что делается руками вне репозитория: прогон DAG на боевых данных, наблюдение за Loki, релиз

## Implementation Steps

### Task 1: Читать `error_type` из тела отказа

**Files:**
- Modify: `airflow_provider_yandex_admetrica/hooks/yandex_admetrica.py`
- Modify: `tests/test_diagnostics.py`
- Modify: `tests/test_hook_http.py`
- Modify: `tests/test_loki.py`
- Modify: `tests/test_token_never_leaves.py`

- [x] расширить `_summarize_error` (`:924`) до тройки `(code, error_type, message)`; `error_type` копируется только при `type(...) is str` и проходит `_safe_text`, значение другого типа описывается своим типом — тем же правилом, что уже применено к `message`
- [x] добавить `"error_type": None` в возвращаемый dict `_new_event` (`:758`); докстринг перечня полей не содержит и правки не требует
- [x] поднять `_SCHEMA_VERSION` (`:44`) до `2`
- [x] обновить оба места вызова `_summarize_error`: `_stamp_response_error` (`:1072`) и путь HTTP 200 с ошибкой в теле (`:2191`)
- [x] заставить `_stamp_response_error` возвращать тройку, которую он проставил: развилка повтора в Task 3 обязана решать по возвращённому значению, а не читать событие обратно — так велит `CONTEXT.md:80–82`
- [x] расширить `_describe_error` (`:953`) до трёх частей так, чтобы тип шёл между кодом и сообщением, а отсутствующая часть выпадала
- [x] обновить **оба** места вызова `_describe_error`: `_with_error` (`:983`) и `_attempt_reason` (`:1041`) — оба сейчас передают два позиционных аргумента
- [x] расширить условия, под которыми фраза вообще собирается: `:982` и `:1040` проверяют только `code` и `message`, поэтому отказ, несущий один лишь `error_type`, был бы разобран и промолчал
- [x] переписать докстринги, называющие прежний контракт как исчерпывающий: `_summarize_error` (`:925`, «only ``code``/``message``»), `_stamp_response_error` (`:1077`), `_find_error` (`:890`, «the first element is the one described, since the event carries one code and one message») и `_describe_error` (`:954–962`)
- [x] в докстринге `_describe_error` отдельно — фраза «the policy for a refusal is decided by the **HTTP status alone**» (`:960–961`). Task 3 делает её ложной: политика решается статусом **и** `error_type`. Это то же самое, что и пропуск докстринга `_request_page`, — перечень текстов под правку выводится из сигнатур, а не из того, чьи утверждения меняются
- [x] добавить `error_type` в `EVENT_KEYS` (`tests/test_diagnostics.py:576`)
- [x] **не** добавлять `error_type` в исключающий набор на `tests/test_diagnostics.py:779`: там перечислены поля, которые при создании события уже определены, а `error_type` начинается как `None` и обязан остаться в проверке «начинается пустым»
- [x] обновить `TestSummarizeError` (`tests/test_diagnostics.py:959`): **восемь** сравнений с кортежем-двойкой (`:961, 975, 985, 988, 991, 994, 1000, 1003`) становятся тройками
- [x] отдельно — **два распаковывания** `code, message = _summarize_error(...)` на `:966` и `:978`: они падают не ассертом, а `ValueError: too many values to unpack (expected 2)`, поэтому счётом сравнений не находятся
- [x] обновить `TestDescribeError` (`:1007`) — четыре вызова с двумя позиционными аргументами
- [x] добавить `"error_type": None` в два события, собранных руками в `tests/test_hook_http.py:419` и `:431`: они перечисляют семь ключей, а `_attempt_reason` начнёт читать восьмой
- [x] добавить `error_type` полной длины в `_full_event` (`tests/test_loki.py:510`) — это заведомо худшее событие, на котором проверяется потолок строки Loki в 256 КБ, и поле на 300 символов эмодзи обязано в него входить
- [x] тест: тело `{"errors":[{"error_type":"query_error","message":"..."}],"code":400,...}` даёт `error_type="query_error"` и `error_code=None`
- [x] тест: `error_type` нестрокового типа описывается типом и не роняет разбор
- [x] тест: тело без `error_type` оставляет поле `None`
- [x] тест утечки: `error_type`, содержащий токен, уходит замаскированным (`tests/test_token_never_leaves.py`)
- [x] тест: `schema_version` события равен 2
- [x] прогнать `.venv/bin/pytest tests/ -q` — зелено перед шагом 2

### Task 2: Отдельная лестница задержек и её инвариант

**Files:**
- Modify: `airflow_provider_yandex_admetrica/hooks/yandex_admetrica.py`
- Modify: `tests/test_hook_http.py`

- [x] добавить `_QUERY_ERROR_DELAYS = [5, 15, 45]` рядом с `_BACKOFF_DELAYS` (`:223`). Комментарий формулируется положительно — «размах лестницы минутный, потому что отказ дрейфует на масштабе минут», — а не через то, чего не умеет соседняя константа
- [x] добавить `_RETRYABLE_ERROR_TYPES = frozenset({"query_error"})`
- [x] тест: `len(_QUERY_ERROR_DELAYS) == len(_BACKOFF_DELAYS)` — номер попытки считается по длине одной из них, и рассогласование сломало бы индексацию ступеней
- [x] прогнать тесты — зелено перед шагом 3

### Task 3: Повторять HTTP 400 с `query_error`

**Files:**
- Modify: `airflow_provider_yandex_admetrica/hooks/yandex_admetrica.py`
- Modify: `tests/test_hook_http.py`

- [x] перенести `_stamp_response_error` в начало ветки `else` (`:2236`), до выбора исхода
- [x] завести внутри неё развилку: 400 с `error_type` из `_RETRYABLE_ERROR_TYPES` получает исход `retryable_error` и ступень из `_QUERY_ERROR_DELAYS`, всё остальное остаётся `http_error` и падает сразу
- [x] на последней попытке повторяемый 400 падает с текстом, называющим и статус, и номер попытки — по образцу ветки `_RETRY_STATUSES`
- [x] **переписать докстринг самой изменяемой функции** — `_request_page` (`:2065–2098`). Сейчас он говорит «A 400 or any other 4xx fails at once too… a repeat brings back the same answer» (`:2078–2080`), «A 401 fails at once» (`:2076`), перечисляет `_BACKOFF_DELAYS` как единственную лестницу (`:2074–2075`) и обещает строку лога только для попытки, не принёсшей страницу (`:2089–2091`). Task 3, 4, 2 и 6 отменяют все четыре утверждения
- [x] соседние тексты того же класса, каждый читается как исчерпывающий и после правок перестаёт им быть: докстринг `_event_level` (`:1265–1267`, перечень повторяемых отказов), комментарий у подножия цикла (`:2299–2300`, «a non-final retryable status»), комментарий про цепочку ветвей (`:2159–2162`) и комментарий у `_BACKOFF_DELAYS` (`:218–222`), написанный так, будто лестница в модуле одна
- [x] тест: 400 `query_error` повторяется четыре раза и делает паузы ровно 5, 15, 45 с
- [x] тест: 400 `query_error`, прошедший со второй попытки, возвращает страницу и таску не роняет
- [x] тест: 400 с другим `error_type` падает с первой попытки, повторов нет
- [x] тест: 400 без читаемого тела падает с первой попытки
- [x] тест: непоследние попытки уходят в Loki с `level="warn"`, последняя — с `level="error"`
- [x] тест: `outcome` повторяемого 400 равен `retryable_error`, а `http_status` остаётся 400 — дашборд отличает его от 5xx по статусу и типу
- [x] **проверка откатом**: отменить развилку, убедиться, что тесты на повтор краснеют, вернуть
- [x] прогнать тесты — зелено перед шагом 4

### Task 4: Отказ авторизации — 401 и 403

**Files:**
- Modify: `airflow_provider_yandex_admetrica/hooks/yandex_admetrica.py`
- Modify: `tests/test_hook_http.py`

- [x] добавить `_AUTH_STATUSES = frozenset({401, 403})` рядом с `_RETRY_STATUSES` (`:216`)
- [x] ветку `401` (`:2202`) перевести на этот набор, а текст исключения — на фактический статус вместо зашитого «401 Unauthorized»
- [x] обновить `tests/test_hook_http.py:281`: там `match="401 Unauthorized"`, а зашитая формулировка из `:2207` уходит
- [x] убрать 403 из `@pytest.mark.parametrize("status", [403, 404, 409, 422])` в `tests/test_hook_http.py:298` — после правки 403 перестаёт быть «любым прочим 4xx». Тест при этом останется зелёным сам по себе, потому что `match=f"returned {status}"` найдётся и в новом тексте, и это хуже падения: набор продолжил бы утверждать то, что уже неверно
- [x] тест: 403 с телом `{"errors":[{"error_type":"invalid_token",...}]}` даёт исход `auth_error` и падает с первой попытки
- [x] тест: сообщение исключения называет полученный статус и имя коннекшена
- [x] тест: 401 ведёт себя ровно так же
- [x] прогнать тесты — зелено перед шагом 5

### Task 5: Строка лога не обещает попыток, которых не будет

**Files:**
- Modify: `airflow_provider_yandex_admetrica/hooks/yandex_admetrica.py`
- Modify: `tests/test_hook_http.py`

- [x] в `_log_attempt` (`:1052`) выводить дробь `attempt N/M` только для исходов из `_RETRIED_OUTCOMES`; для остальных строка называет окончательность отказа
- [x] обновить докстринг так, чтобы он описывал обе формы строки
- [x] тест: 400 с неповторяемым `error_type` даёт строку без дроби и без `Retrying in`
- [x] тест: 403 даёт строку без дроби
- [x] тест: 503 и повторяемый 400 дробь сохраняют — существующие тесты `TestAttemptLines` (`tests/test_hook_http.py:364`) обязаны остаться зелёными без правок
- [x] **проверка откатом**: вернуть безусловную дробь, убедиться, что новые тесты краснеют
- [x] прогнать тесты — зелено перед шагом 6

### Task 6: Строка о том, что повтор помог

**Files:**
- Modify: `airflow_provider_yandex_admetrica/hooks/yandex_admetrica.py`
- Modify: `tests/test_hook_http.py`

- [x] в блоке `finally` у `_request_page` (`:2271`) писать одну INFO-строку при успехе после неуспешных попыток, с тем же префиксом `_describe_target`
- [x] разместить её **внутри** существующего `try` (`:2278`), рядом с `_log_attempt`: его `except Exception` (`:2287`) затем и стоит, чтобы отчётность не могла подменить ни значение, ни исключение в полёте — снаружи этой защиты сбой при сборке строки съел бы возвращаемую страницу
- [x] на первой удачной попытке не писать ничего — здоровый прогон лог не меняет
- [x] строка не должна ломать существующий подсчёт строк: тесты `TestAttemptLines` считают WARNING, и INFO не должен в них попасть
- [x] тест: успех со второй попытки даёт ровно одну WARNING и ровно одну INFO с номером попытки
- [x] тест: успех с первой попытки не даёт ни одной строки
- [x] тест: полностью проваленный запрос INFO-строки не даёт
- [x] **проверка откатом**: убрать строку, убедиться, что тесты краснеют
- [x] прогнать тесты — зелено перед шагом 7

### Task 7: `include_undefined` — аннотация в согласии с поведением

**Files:**
- Modify: `airflow_provider_yandex_admetrica/operators/stats.py`
- Modify: `airflow_provider_yandex_admetrica/hooks/yandex_admetrica.py`
- Modify: `tests/test_operator.py`
- Modify: `tests/test_stats.py`

- [x] заменить `include_undefined: bool = True` в `get_stats` (`hooks/yandex_admetrica.py:2505`) на `bool | None = True`: оператор передаёт значение туда напрямую, поэтому правка только в операторе оставила бы публичный вход хука запрещающим то, что код принимает
- [x] заменить `include_undefined: bool = True` (`operators/stats.py:129`) на `bool | None = True`: хук уже принимает `bool | None` и при `None` не отправляет параметр вовсе (`hooks/yandex_admetrica.py:2631`), так что аннотация запрещает то, что код делает
- [x] описать это в докстринге оператора: `None` означает «не отправлять параметр», то есть взять умолчание API
- [x] тест: `include_undefined=None` не кладёт параметр в запрос
- [x] тест: `True` и `False` уходят как `"true"` и `"false"`
- [x] прогнать тесты — зелено перед шагом 8

### Task 8: Докстринг про `sort` — отделить факт от страховки

**Files:**
- Modify: `airflow_provider_yandex_admetrica/hooks/yandex_admetrica.py`

- [x] переписать абзац про `sort` в докстринге `get_stats` (`:2524`) так, чтобы было видно: документация AdMetrica о порядке строк при постраничном обходе не говорит ничего, а сортировка по группировкам — наша страховка от недокументированного, а не следствие описанного поведения
- [x] назвать, чем страховка подкреплена: детектор повторов строк (`:2757`) и сверка с `total_rows` (`_check_row_total`, `:2800`) ловят разъехавшиеся страницы, так что нестабильный порядок был бы упавшим днём, а не дыркой в данных
- [x] упомянуть щель: при `total_rows_rounded` сверка смягчается до WARNING
- [x] написать текст в настоящем времени. Формулировки вида «докстринг больше не утверждает, что порядок гарантирован» правило проекта запрещает: описывается то, что код делает сейчас, а не то, чем текст был раньше
- [x] тестов не требуется — правка только в тексте; прогнать тесты для страховки

### Task 9: Документация в согласии с новым поведением

**Files:**
- Modify: `README.md`
- Modify: `README_RU.md`
- Modify: `CONTEXT.md`

- [x] таблица отказов (`:251–254` в обоих README): строку про 400 разделить на повторяемый `query_error` и остальные 4xx; строку про 401 расширить до 401 и 403
- [x] бюджет долгой таски (`:83`): повторяемый 400 добавляет одному запросу 65 с пауз **плюс три запроса**, каждый ограничен `_REQUEST_TIMEOUT` в 30 с и эмпирически стоит около 10 с, когда упирается в дедлайн, — худший случай около 155 с. Абзац существует ради подбора `execution_timeout`, и занижение важно именно здесь
- [x] `schema_version` (`:361`): версия `2`
- [x] строка про `error_code`/`error_message` (`:383`): добавить `error_type` и сказать, откуда он берётся и почему `error_code` для этого API обычно пуст
- [x] таблица `outcome` (`:411–413`): `auth_error` — 401 и 403; `retryable_error` — плюс 400 с `query_error` по своей лестнице
- [x] раздел про гарантию по токену (`:458`): `error_type` — ещё один канал, покрытый тем же шлюзом
- [x] `:370` — «`attempt` counts from 1 up to `max_attempts` as 429s, 5xx answers and network failures are retried out of one budget». Это закрытое перечисление того, что тратит бюджет попыток, и Task 3 добавляет четвёртый случай
- [x] секция «Task log» `:272–282` — здесь документирована ровно та строка, которую меняют Task 5 и 6. Утверждение `:272` «каждая неуспешная попытка оставляет одну строку» расширяет Task 6. В фенсе `:274–280` правки требует **только `:279`** — это `empty_shape`, и после Task 5 дробь оттуда уходит; `:275–278` несут `retryable_error` и `network_error`, оба входят в `_RETRIED_OUTCOMES` (`hooks/yandex_admetrica.py:241`) и дробь сохраняют
- [x] `:466` — говорит, что из отказа извлекаются только `code` и `message`
- [x] `CONTEXT.md:20` — всё ещё `_SCHEMA_VERSION = 1`
- [x] раздел «чего мы не знаем об API» (`:287–289`): записать установленное опытом — 400 `query_error` транзиентен, сообщение про сложность запроса не соответствует наблюдаемому, ни `accuracy`, ни число группировок на исход не влияют, порог около десяти секунд
- [x] таблица параметров оператора: `include_undefined` принимает `None`
- [x] `CONTEXT.md`, по адресам, а не «шов в новых терминах»: `:8` — 401 и 403 как окончательный отказ; `:35` — что выяснено про формат тела ошибки; `:70–72` — «ровно одно событие и, для неуспешной попытки, ровно одна строка в логе», расширяется Task 6; `:80–82` — правило шва про решение по возвращённому значению, остаётся в силе и подпирает форму Task 3; `:331` — `error_type` в таблице маскирования; `:352` — строка про `_BACKOFF_DELAYS = [1, 2, 4]` как единственную лестницу; `:354` — строка таблицы про 401, расширяется до 403; `:355` — «400 и прочие 4xx — `AirflowException` сразу»
- [x] прогнать `.venv/bin/pytest tests/test_readme.py -q`. **Барьер неполон и на него нельзя опираться**: эти тесты проверяют python-блоки, таблицы параметров, таблицу именования и перекрёстные ссылки, но фенс с примером строки лога для них обычный код-блок, а `CONTEXT.md` не стережёт ничто. Перечисленные выше адреса проверяются глазами
- [x] прогнать полный набор тестов

### Task 10: Проверка критериев приёмки

- [x] 400 `query_error` повторяется четырьмя попытками по лестнице 5 / 15 / 45 с, и таска переживает единичный отказ кампании
- [x] непоследние попытки уходят в Loki как `warn`, последняя как `error`
- [x] каждая строка лога называет кампанию, день и страницу; успешный повтор оставляет строку о восстановлении
- [x] 403 диагностируется как мёртвый токен наравне с 401
- [x] неповторяемый отказ не обещает в логе попыток, которых не будет
- [x] событие несёт `error_type` и `schema_version: 2`, ни одно прежнее поле не переименовано и не удалено
- [x] `.venv/bin/pytest tests/ -q` — все тесты зелёные, включая 937 прежних
- [x] `.venv/bin/python -m build && .venv/bin/twine check dist/*` — пакет собирается и проходит проверку (сборка велась во временный каталог, оба артефакта PASSED)

### Task 11: [Финал] Документация и закрытие плана

- [x] обновить `CHANGELOG.md`: раздел под следующую версию с перечнем изменений поведения и `schema_version` 1 → 2
- [x] перенести этот план в `docs/plans/completed/` (перенос делает харнесс после всех фаз)

## Post-Completion

*Требует ручных действий вне репозитория — без чекбоксов*

**Проверка на боевых данных:**

- прогнать `admetrica_to_s3` за период, на котором раньше падало, и убедиться, что дни доходят до конца
- посмотреть в Loki долю событий с `outcome="retryable_error"` и `http_status=400` — она же оценка `p`, из которой считалась вероятность успеха прогона
- если доля окажется заметно выше нескольких процентов, лестницу `[5, 15, 45]` придётся пересматривать: при `p = 10 %` четыре попытки дают уже 97 %, а не пять девяток
- третья величина — **дни, упавшие на «сменившемся под обходом итоге»**. Лестница
  растягивает окно повторов внутри постраничного обхода с 7 до 65 секунд, а
  `query_error` приходит именно на больших отчётах в несколько страниц, поэтому
  кампания-день, у которой `total_rows` меняется за эти 65 секунд, падает с этой
  формулировкой заметно чаще прежнего. Падение громкое и правильное, но его доля в Loki
  — то, по чему видно, не пора ли платить за повтор целым обходом кампании вместо одной
  страницы
- вторая величина, за которой смотреть, — **корреляция попыток**: сколько запросов исчерпали все четыре против того, сколько восстановились. Расчёт вероятности исходит из независимости, а лестница в 65 с лежит внутри минутного дрейфа; много полных исчерпаний при малой общей доле отказов означает, что независимости нет и лестницу надо растягивать, а не удлинять

**Вернуть параметры примера DAG:**

- `accuracy` и урезанные `DIMENSIONS`/`METRICS` менялись в ходе диагностики и ни на что не влияют; вернуть прежние значения

**Наблюдение:**

- дашборд по `error_type` покажет, есть ли у этого API отказы других типов, которые тоже стоит повторять
- строки `recovered on attempt N/M` — материал для оценки того, достаточна ли длина лестницы

**Релиз:**

- версия и тег после того, как поведение подтвердится на боевых данных
- публикация на PyPI требует настроенного pending publisher и окружения `pypi` в GitHub — это отдельная ручная настройка, не сделанная до сих пор
